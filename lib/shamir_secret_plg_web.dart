import 'package:flutter_web_plugins/flutter_web_plugins.dart';

/// Web plugin stub (no native implementation needed)
class ShamirSecretPlgWeb {
  static void registerWith(Registrar registrar) {
    // No-op: Pure Dart works on all platforms
  }
}
